# 广义连续 SOS 模型的蒙特卡洛模拟方案

本文档定义了二维正方晶格周期性边界条件上带有广义幂律相互作用的连续 Solid-On-Solid (SOS) 模型，并详细列出了在蒙特卡洛 (Monte Carlo) 模拟中需要测量的核心物理量。

## 1. 模型定义与模拟前提

我们考虑一个尺寸为 $L \times L$ 的二维正方晶格，系统总格点数为 $N = L^2$。采用周期性边界条件 (Periodic Boundary Conditions, PBC)。



* **高度场变量**：每个格点 $i$ 上的高度 $h_i$ 为连续实数，即 $h_i \in \mathbb{R}$。
* **哈密顿量**：系统中相邻格点的高度差贡献能量，哈密顿量定义为广义的 $\sigma$ 次幂形式：
  $$\mathcal{H} = K \sum_{\langle i, j \rangle} |h_i - h_j|^{\sigma}$$
  其中：
  * $\langle i, j \rangle$ 表示对所有最近邻格点对求和。
  * $K$ 为耦合常数（模拟中可将温度 $T$ 吸收，设定约化参数 $K/T$）。
  * $\sigma$ 为大于 0 的实数。本次模拟重点关注 $\sigma = 1, 2, 3$ 这三种情况。
* **零模消除约束**：为了解决全局平移不变性带来的高度发散问题，在模拟的每一步测量中，严格保证系统整体的质心高度为 0，即强制满足：
  $$\sum_{i=1}^{N} h_i = 0$$

---

## 2. 测量的物理量 (严格定义)

在系统达到热力学平衡后，通过蒙特卡洛步骤的系综平均（记为 $\langle \cdots \rangle$）来提取以下物理量。

### 2.1 实空间观测量

**1. 表面粗糙度 / 界面宽度 ($W^2$)**
由于已经施加了 $\sum_i h_i = 0$ 的约束，瞬时平均高度 $\bar{h}$ 恒为 0，因此粗糙度的定义简化为各格点高度平方的平均值：
$$W^2(L) = \left\langle \frac{1}{N} \sum_{i=1}^{N} h_i^2 \right\rangle$$

**2. 高度-高度关联函数 ($G(\mathbf{r})$)**
定义为空间上相距矢量 $\mathbf{r}$ 的两个格点高度差的平方期望：
$$G(\mathbf{r}) = \left\langle \frac{1}{N} \sum_{i=1}^{N} [h(\mathbf{r}_i + \mathbf{r}) - h(\mathbf{r}_i)]^2 \right\rangle$$
*(注：即使相互作用是 $\sigma$ 次幂，关联函数在文献中通常仍定义为高度差的平方，以便与标准的粗糙度 $W^2$ 定义在量纲上保持一致。)*

### 2.2 动量空间观测量

定义高度场 $h_i$ 的离散傅里叶变换（归一化模式）为：
$$h_{\mathbf{k}} = \frac{1}{\sqrt{N}} \sum_{j=1}^{N} h_j e^{-i \mathbf{k} \cdot \mathbf{r}_j}$$

**1. 结构因子 / 极化率 ($\chi_{\mathbf{k}}$)**
定义为动量空间中非零波矢 $\mathbf{k}$ 模式的模长平方期望：
$$\chi_{\mathbf{k}} = \langle |h_{\mathbf{k}}|^2 \rangle \quad (\mathbf{k} \neq \mathbf{0})$$

### 2.3 热力学观测量

**1. 平均能量密度 ($e$)**
$$e = \frac{\langle \mathcal{H} \rangle}{N}$$

**2. 比热容 ($C_v$)**
反映能量的涨落程度：
$$C_v = \frac{\langle \mathcal{H}^2 \rangle - \langle \mathcal{H} \rangle^2}{N T^2}$$

---

## 3. 物理意义与理论预期

测量上述物理量的核心目的，是确定系统在不同 $\sigma$ 值下的宏观相以及有限尺寸标度行为 (Finite-Size Scaling)。

### 3.1 表面粗糙度 $W^2(L)$ 与关联函数 $G(\mathbf{r})$ 的意义
这两个量用于判断界面处于“平滑相”(Smooth/Flat) 还是“粗糙相”(Rough)。
* **$\sigma = 2$ (标准连续高斯模型)**：理论上已被严格证明，$W^2(L) \propto \ln L$ 且 $G(\mathbf{r}) \propto \ln |\mathbf{r}|$。系统在任何有限温度下都处于对数发散的粗糙相。
* **$\sigma \neq 2$ 的探索**：当相互作用变为绝对值线性势 ($\sigma = 1$) 或更高次幂 ($\sigma = 3$) 时，大尺度下的涨落行为是未知的或难以精确解析的。通过拟合 $W^2(L)$ 随尺寸 $L$ 的变化（例如它是趋于常数、对数发散，还是代数发散 $L^\alpha$），可以确定表面在不同势场限制下的真实形貌。

### 3.2 结构因子 $\chi_{\mathbf{k}}$ 的意义
* **提取有效长波行为**：对于 $\sigma = 2$，$\chi_{\mathbf{k}}$ 在长波极限 ($k \to 0$) 下表现为 $1/k^2$ 发散（戈德斯通模式）。对于 $\sigma = 1, 3$，通过在双对数坐标下绘制 $\chi_{\mathbf{k}}$ 对 $k$ 的关系，可以测量出等效的关联指数。如果它依然保持 $1/k^2$ 的发散，说明在重整化群的意义下，大尺度物理仍属于高斯普适类。
* **提取有效刚度**：通过测量最小可用波矢 $\mathbf{k}_{min} = (2\pi/L, 0)$ 处的 $\chi_{\mathbf{k}_{min}}$，可以提取系统在宏观尺度下表现出的等效表面张力 $K_{eff}$。

### 3.3 热力学的意义
由于 $\sigma \neq 2$ 时模型没有平凡的解析解，能量 $e$ 和比热 $C_v$ 主要用于：
1. **算法校验**：通过对比不同尺寸 $L$ 下的能量收敛情况，确认蒙特卡洛热浴是否达到了真正的热力学平衡。
2. **相变探测**：如果系统在某个温度或改变 $\sigma$ 的过程中存在潜在的结构相变（对于连续模型较少见，但值得警惕），比热容 $C_v$ 会在相变点表现出峰值或发散。

## 4. 蒙特卡洛模拟算法

为了使系统在给定的哈密顿量 $\mathcal{H} = K \sum_{\langle i, j \rangle} |h_i - h_j|^{\sigma}$ 下达到热力学平衡，我们通常结合使用局部更新和全局更新算法。

### 4.1 Metropolis 局部更新算法

（作为基础的试探性随机漫步算法，通过在单个格点 $i$ 上引入随机微扰 $h_i \to h_i + \delta h$ 并根据 $e^{-\Delta \mathcal{H}}$ 决定接受与否。此处仅作名称占位，具体实现细节略。）

### 4.2 Swendsen-Wang 集团更新算法 (Cluster Update)



对于存在严重长波涨落的连续系统，单纯的 Metropolis 算法会遭遇严重的**临界慢化** (Critical Slowing Down)。为此，我们可以引入类似 Ising 模型的 Swendsen-Wang 集团算法。

由于我们的高度场 $h_i$ 是连续实数，类比于 Ising 模型中自旋的正负，我们可以利用系统在**全局符号反转**下的对称性：即如果把所有格点的 $h_i$ 变成 $-h_i$，哈密顿量 $\mathcal{H}$ 保持不变（因为 $|-h_i - (-h_j)|^\sigma = |h_i - h_j|^\sigma$）。

#### 放键概率 (Bond Probability) 的推导

集团算法的核心在于满足**细致平衡条件** (Detailed Balance)。对于相邻的两个格点 $i$ 和 $j$，我们考虑将其中一个格点的符号反转带来的能量惩罚。

1.  **同处于一个集团 (不相对翻转) 的能量：**
    如果 $h_i$ 和 $h_j$ 绑定在同一个集团中（要么都不变，要么同时翻转符号），它们之间的相互作用能为：
    $$E_{same} = K |h_i - h_j|^\sigma$$

2.  **处于不同集团 (相对翻转) 的能量：**
    如果它们被分在不同的集团中，在某次更新中只有一个翻转了符号（例如 $h_i \to -h_i$），那么它们之间的相互作用能变为：
    $$E_{diff} = K |-h_i - h_j|^\sigma = K |h_i + h_j|^\sigma$$

3.  **能量差与放键概率：**
    根据 Fortuin-Kasteleyn (FK) 映射和细致平衡，为了消除翻转集团时造成的能量壁垒，我们需要以一定的概率 $p_{ij}$ 在 $i$ 和 $j$ 之间“放键”将它们冻结在一起。这个概率取决于翻转带来的能量差 $\Delta E = E_{diff} - E_{same}$。
    
    放键概率的严格表达式为：
    $$p_{ij} = \max\left(0, 1 - e^{-\Delta E}\right) = \max\left(0, 1 - \exp\left[-K \left( |h_i + h_j|^\sigma - |h_i - h_j|^\sigma \right)\right]\right)$$

#### 物理分析与放键规则

观察上述表达式中的能量差 $\Delta E = K \left( |h_i + h_j|^\sigma - |h_i - h_j|^\sigma \right)$：

* **当 $h_i$ 和 $h_j$ 同号时 ($h_i h_j > 0$)：** 显然有 $|h_i + h_j| > |h_i - h_j|$，因此 $\Delta E > 0$。此时存在断键的能量惩罚，我们**放键**，且放键概率为：
    $$p_{ij} = 1 - \exp\left[-K \left( |h_i + h_j|^\sigma - |h_i - h_j|^\sigma \right)\right]$$
* **当 $h_i$ 和 $h_j$ 异号时 ($h_i h_j \le 0$)：** 此时 $|h_i + h_j| \le |h_i - h_j|$，导致 $\Delta E \le 0$。这意味着相对翻转反而会降低能量，因此在此情况下我们**绝对不放键**：
    $$p_{ij} = 0$$

#### 与标准 Ising 模型的关键区别

在经典的 Ising 模型中，$s_i \in \{+1, -1\}$，如果 $s_i$ 和 $s_j$ 同号，能量差固定为 $\Delta E = 2J$，放键概率是一个常数 $p = 1 - e^{-2J}$。

但在当前的连续 SOS 模型中，格点场的大小 $|h_i|$ 不是固定的 1。因此，**放键概率 $p_{ij}$ 变成了一个局部依赖的动态变量**，它不仅要求相邻格点同号，还会根据 $|h_i|$ 和 $|h_j|$ 的具体数值进行动态调整。相邻格点的绝对高度越大，它们同号时的放键概率越接近 1。

---

> **⚠️ 关键修正提醒：关于“零模约束”与防止数值溢出**
> 
> 在引入集团算法或单纯的 Metropolis 演化时，系统的整体平均高度（零模）会发生随机游走。如果任由其无限制地漂移，长时间模拟必然会导致浮点数溢出。
> 
> **标准工程解决方案**：
> 1. **演化阶段的定期平移**：在马尔可夫链演化过程中（例如每完成一次全晶格的 Sweep），计算当前的平均高度 $\bar{h} = \frac{1}{N} \sum_i h_i$，并强制将所有格点高度进行平移 $h_i \leftarrow h_i - \bar{h}$。这既不会改变系统的任何能量和物理状态（得益于平移对称性），又能将场值始终限制在安全的数值范围内。
> 2. **测量阶段的基准**：在每次采样测量物理量（如 $W^2$ 和 $G(\mathbf{r})$）时，同样确保使用减去瞬时均值后的场值进行计算，以保证数据的绝对干净。

---

## 5. 模拟参数与 Benchmark 方案

为了全面评估广义连续 SOS 模型的物理性质以及算法的效率，本次模拟设定了以下参数和对比基准。

### 5.1 物理与系统参数
* **系统尺寸 ($L$)**：分别取 8, 16, 32, 64。多尺寸的数据将用于有限尺寸标度分析 (Finite-Size Scaling)，提取临界指数和长波行为。
* **相互作用指数 ($\sigma$)**：
    * $\sigma = 1$：线性绝对值势。
    * $\sigma = 2$：标准高斯势（用于验证代码正确性并与解析解对比）。
    * $\sigma = 3$：三次幂势（强非线性惩罚）。

### 5.2 算法 Benchmark 对比
由于此类连续场模型在大尺度下存在强烈的长波涨落，系统极易陷入**临界慢化** (Critical Slowing Down)。为了验证集团算法的加速效果，我们将对以下两种更新策略进行严格的 Benchmark：
1. **纯 Metropolis 更新 (Baseline)**：仅使用局部的单自旋试探更新。
2. **混合更新 (Metropolis + Swendsen-Wang)**：在执行若干步局部 Metropolis 更新后，插入一次全局的 Swendsen-Wang 集团翻转更新。

**对比指标**：
* 两种方法在相同 MC 步数下达到的热力学平均值是否一致（验证细致平衡和代码正确性）。
* 物理量（如能量 $e$ 或粗糙度 $W^2$）的**自相关时间** (Autocorrelation time, $\tau$)。期望混合更新在高 $\sigma$ 和大 $L$ 下能显著降低 $\tau$。

### 5.3 编程语言与技术栈
* **语言**：**Julia**
